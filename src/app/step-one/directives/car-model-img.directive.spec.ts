import { CarModelImgDirective } from './car-model-img.directive';

describe('CarModelImgDirective', () => {
  it('should create an instance', () => {
    const directive = new CarModelImgDirective();
    expect(directive).toBeTruthy();
  });
});
